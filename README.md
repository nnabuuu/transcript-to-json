# Transcript to JSON with OpenAI GPT-4o

This project converts classroom transcripts into structured JSON with role (teacher/student) probabilities.

## Usage

1️⃣ Install dependencies:

```bash
npm install

